import 'package:flutter/material.dart';

class ChattingScreen extends StatelessWidget {
  const ChattingScreen();

  @override
  Widget build(BuildContext context) {
    print(ChattingScreen);
    return Scaffold(
      body: Center(child: Text("ChattingScreen")),
    );
  }
}
